# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Redis
redis==5.0.1

# HTTP client
requests==2.31.0
aiohttp==3.9.1
aiofiles==23.2.1

# Brotli decompression support for requests
brotli==1.1.0

# Browser-like HTTP client with TLS fingerprint impersonation
# This bypasses TLS fingerprinting (JA3) anti-bot detection
curl_cffi==0.7.1

# M3U8 parsing
m3u8==3.5.0

# Encryption (AES-128 for HLS)
pycryptodome==3.19.0

# Utilities
python-dotenv==1.0.0

